# Production runtime deps only. Test-only deps live in requirements-dev.txt; the
# Dockerfile installs this file alone (.dockerignore excludes tests/).
httpx==0.28.1
psycopg[binary]==3.3.4
# Connection pool for app.py - separate distribution from psycopg itself. Without
# it every request pays a fresh connect + TLS handshake + auth to Neon.
psycopg-pool==3.3.1
numpy==2.4.6
python-dotenv==1.2.3
lightgbm==4.7.0
fastapi==0.141.1
uvicorn==0.52.4
# python:*-slim ships without a tz database; Asia/Kolkata (vericast/local_time.py)
# fails without this.
tzdata==2026.3
