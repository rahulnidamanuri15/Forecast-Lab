# Test-only deps, split out so the production image ships no test runner.
#
# Install both for local work and in CI:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==9.1.1
